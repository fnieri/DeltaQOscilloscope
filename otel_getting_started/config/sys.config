[
    {opentelemetry, [
        {tracer_provider, [
            {exporters, [
                {otlp, #{protocol => http_json, endpoints => ["http://localhost:4318"]}} 
            ]}
        ]}
    ]}
].
