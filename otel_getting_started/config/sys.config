[
    {opentelemetry, [
        {tracer_provider, [
            {exporters, [
                {otlp, #{protocol => http, endpoints => ["http://localhost:4318", "http://localhost:8080"]}} 
            ]}
        ]}
    ]}
].
